# Database Configuration
MONGODB_URL=mongodb://localhost:27017/carrerAdvisor
# For production, use MongoDB Atlas or Cloud SQL
# MONGODB_URL=mongodb+srv://username:password@cluster.mongodb.net/carrerAdvisor

# JWT Configuration
JWT_SECRET=your_jwt_secret_key_here
JWT_EXPIRES_IN=7d

# Google Cloud Configuration
GOOGLE_CLOUD_PROJECT_ID=your-project-id
GOOGLE_APPLICATION_CREDENTIALS=/app/config/service-account.json

# Vertex AI Configuration (if using)
PROJECT_ID=your-project-id
LOCATION=us-central1
ENDPOINT_ID=your-endpoint-id

# Server Configuration
PORT=8080
NODE_ENV=production

# CORS Configuration
ALLOWED_ORIGINS=http://localhost:5173,https://carrer-advisor-project.vercel.app
